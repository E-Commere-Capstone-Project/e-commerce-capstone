/*
Needs imports to be able to work: useEffect and useNavigate at minimum
*/

export default function Profile() {
  /*

  use a useEffect to be able to display the profile data for the logged in user
  useEffect(()=>{
    async function profileFetch() {
      try {
        const data = await fetchProfile(userToken);

        return data;
      } catch(err) {
         console.error(err);
      }
    }
  },[]);

  return (
    <>
      display relevant profile data
      edit user data (email, name, password, username)
    </>
  )
  */
}
